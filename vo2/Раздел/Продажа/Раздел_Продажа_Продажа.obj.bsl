
&НаКлиенте
Перем МестныйКэш;

#Область include_local_ПолучитьМодульОбъекта
#КонецОбласти

&НаКлиенте
Функция сбисЭлементФормы(Форма,ИмяЭлемента)
	Если ТипЗнч(ЭтаФорма) = Тип("УправляемаяФорма") Тогда
		Возврат Форма.Элементы.Найти(ИмяЭлемента);
	КонецЕсли;
	Возврат Форма.ЭлементыФормы.Найти(ИмяЭлемента);
КонецФункции

&НаКлиенте
Функция сбисПолучитьСтраницу(Элемент, ИмяСтраницы)
	Если ТипЗнч(ЭтаФорма) = Тип("УправляемаяФорма") Тогда
		Возврат Элемент.ПодчиненныеЭлементы[ИмяСтраницы];
	КонецЕсли;
	Возврат Элемент.Страницы[ИмяСтраницы];
КонецФункции

&НаКлиенте
Функция ОбновитьКонтент(Кэш, ДопПараметры = Неопределено) Экспорт
// Функция выполняет переход в первый подраздел раздела Продажа	
	Если Не ЗначениеЗаполнено(Кэш) Тогда
		Возврат Ложь;
	КонецЕсли;
	Возврат Кэш.ГлавноеОкно.ПерейтиВРаздел("АккордеонПродажаДок033");
КонецФункции

&НаКлиенте
Процедура НаСменуРаздела(Кэш) Экспорт
// Процедура обновляет панель массовых операций, панель фильтра, контекстное меню при смене раздела
	
	ГлавноеОкно = Кэш.ГлавноеОкно;
	МодульСпискаСостоянийДокумента = МодульОбъектаКлиент().НайтиФункциюСеансаОбработки("СписокСостоянийДокумента",
	Кэш.Текущий.ОписаниеРаздела.ФормыРаздела);
	ГлавноеОкно.СписокСостояний = МодульСпискаСостоянийДокумента.СписокСостоянийДокумента();

	ГлавноеОкно.ФильтрОбновитьПанель();

	ГлавноеОкно.сбисУстановитьКонтекстноеМеню("Таблица_РеестрДокументов", "КонтекстноеМенюПродажа");
	ПанельМассовыхОпераций = сбисЭлементФормы(ГлавноеОкно,"ПанельМассовыхОпераций");
	ПанельМассовыхОпераций.ТекущаяСтраница = сбисПолучитьСтраницу(ПанельМассовыхОпераций,"Продажа");
	ГлавноеОкно.сбисЭлементФормы(ГлавноеОкно,"ПанельМассовыхОпераций").Видимость = Истина;//aa.uferov раздел задач
		
	ГлавноеОкно.сбисЭлементФормы(ГлавноеОкно,"ПанельТулбар").Видимость = Истина;
	ГлавноеОкно.сбисЭлементФормы(ГлавноеОкно,"ПанельФильтра").Видимость = Истина;
	ГлавноеОкно.сбисЭлементФормы(ГлавноеОкно,"ПоказатьПанельФильтра").Видимость = Истина;
	ГлавноеОкно.сбисЭлементФормы(ГлавноеОкно,"Таблица_РеестрДокументов.ТекущийЭтап").Видимость = Ложь; // alo_ТекущийЭтап
	
	ДопОперации2       = МодульОбъектаКлиент().ПолучитьЭлементФормыОбработки(ГлавноеОкно, "ДопОперации2");
	ФильтрМаскаПродажа = МодульОбъектаКлиент().ПолучитьЭлементФормыОбработки(ГлавноеОкно, "ФильтрМаскаПродажа");
	
	Если ДопОперации2.Видимость Тогда
		ОтСтупСлева = 206; // на момент 2100 относительно ДопОперации2 (изменить при необходимости на привязку к элементу)
	Иначе
		ОтСтупСлева = 162; // на момент 2100 относительно ДопОперации2 (изменить при необходимости на привязку к элементу)
	КонецЕсли;
	
	ИзмененияЭлемента = Новый Структура("ИзменитьПоложение", ОтСтупСлева);
	МодульОбъектаКлиент().ИзменитьЭлементФормыОбработки(ФильтрМаскаПродажа, ИзмененияЭлемента);
	
КонецПроцедуры

&НаКлиенте
Процедура НавигацияУстановитьПанель(Кэш) Экспорт
// Процедура устанавливает панель навигации на 1ую страницу и скрывает панель	
	ГлавноеОкно = Кэш.ГлавноеОкно;
	сбисЭлементФормы(ГлавноеОкно,"ПанельНавигации").Видимость=Истина;
	сбисЭлементФормы(ГлавноеОкно,"ЗаписейНаСтранице1С").Видимость=Истина;
	сбисЭлементФормы(ГлавноеОкно,"ЗаписейНаСтранице").Видимость=Ложь;
КонецПроцедуры	

#Область include_local_Раздел_Продажа_Продажа
#КонецОбласти