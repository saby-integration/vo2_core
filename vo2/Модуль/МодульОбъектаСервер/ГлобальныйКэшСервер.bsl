
// Процедура - обновляет параметр глобального модуля на сервере
//
// Параметры:
//  ИмяПараметраМодуля	 - Строка	 - имя обновляемого параметра
//  АдресПараметраМодуля - ФиксированнаяСтруктрура	 - Тип и Значение
//
Процедура ОбновитьПараметрГлобальногоМодуляНаСервере(ИмяПараметраМодуля, АдресПараметраМодуля = Неопределено) Экспорт
	
	Если ИмяПараметраМодуля = "ОчиститьГлобальныйКэш" Тогда
		
		ОчиститьГлобальныйКэшНаСервере();
		Возврат;
		
	КонецЕсли;
	
	Если АдресПараметраМодуля = Неопределено Тогда
		
		ЗначениеПараметраМодуля = Неопределено;
		
	ИначеЕсли ТипЗнч(АдресПараметраМодуля) = Тип("ФиксированнаяСтруктура") Тогда
		
		Если АдресПараметраМодуля.Тип = "Адрес" Тогда
	
			ЗначениеПараметраМодуля = ПолучитьИзВременногоХранилища(АдресПараметраМодуля.Значение);
			
		Иначе
			
			ЗначениеПараметраМодуля = АдресПараметраМодуля.Значение;
			
		КонецЕсли;
		
	Иначе
		
		ЗначениеПараметраМодуля = АдресПараметраМодуля;
		
	КонецЕсли;
	
	Если ИмяПараметраМодуля = "ГлобальныйКэш" Тогда
		
		ГлобальныйКэш = ЗначениеПараметраМодуля;
		СохранитьГлобальныйКэшНаСервере();
		Возврат;
		
	КонецЕсли;
	
	ГлобальныйКэш		= ПолучитьГлобальныйКэшНаСервере();
	ЗначениеЗаписать	= ГлобальныйКэш;
	КлючЗаписать		= "ГлобальныйКэшСервер";
	
	Если	ИмяПараметраМодуля = "Парам"			Тогда
		
		Для Каждого КлючИЗначениеПараметра Из ЗначениеПараметраМодуля Цикл
			
			ГлобальныйКэш.Парам.Вставить(КлючИЗначениеПараметра.Ключ, КлючИЗначениеПараметра.Значение);
			
		КонецЦикла;
		
	ИначеЕсли	ИмяПараметраМодуля = "ПараметрыСистемы"	Тогда
		
		ГлобальныйКэш.Вставить("ПараметрыСистемы", ЗначениеПараметраМодуля);
		
	ИначеЕсли	ИмяПараметраМодуля = "КэшИни"	Тогда

		ЗначениеЗаписать	= Новый Структура("Данные", Новый Структура);
		КлючЗаписать		= "КэшИниСервер";

	ИначеЕсли	ИмяПараметраМодуля = "Ини"	Тогда

		КлючЗаписать		= "КэшИниСервер";
		ЗначениеЗаписать	= ПрочитатьГлобальныйПараметрНаСервере(КлючЗаписать);
		Для Каждого КлючИЗначениеИни Из ЗначениеПараметраМодуля Цикл
			
			ЗначениеЗаписать.Данные.Вставить(КлючИЗначениеИни.Ключ, КлючИЗначениеИни.Значение);
			
		КонецЦикла;
		
	КонецЕсли;
	
	СохранитьГлобальныйПараметрНаСервере(КлючЗаписать, ЗначениеЗаписать);
	
КонецПроцедуры

Функция ПолучитьГлобальныйКэшНаСервере()
	
	Если ГлобальныйКэш = Неопределено Тогда
		
		ВосстановитьГлобальныйКэшНаСервере();
		
	КонецЕсли;
	
	Возврат ГлобальныйКэш;
	
КонецФункции

Процедура ОчиститьГлобальныйКэшНаСервере()
	
	// Ничего не трогаем, если что кэш просто пересоздадим.
	Если ЭтоСерверУправляемоеПриложение() Тогда
		
		//УстановитьПривилегированныйРежим(Истина);

		//Попытка
		//	
		//	ХранилищеОбщихНастроек.Удалить("Integration", "ГлобальныйКэшСервер", "Integration");
		//	
		//Исключение
		//	
		//	//Обёрнуто скорее на всякий случай, чтобы дальнейшее закрытие не падало.
		//	
		//КонецПопытки;
		//
		//УстановитьПривилегированныйРежим(Ложь);
		
	КонецЕсли
		
КонецПроцедуры

Процедура СохранитьГлобальныйКэшНаСервере() Экспорт
	
	СохранитьГлобальныйПараметрНаСервере("ГлобальныйКэшСервер", ГлобальныйКэш)
		
КонецПроцедуры

Процедура ВосстановитьГлобальныйКэшНаСервере()

	ГлобальныйКэш = ПрочитатьГлобальныйПараметрНаСервере("ГлобальныйКэшСервер");
		
КонецПроцедуры

Процедура СохранитьГлобальныйПараметрНаСервере(КлючПараметра, ЗначениеПараметра)
	
	Если ЭтоСерверУправляемоеПриложение() Тогда
		
		УстановитьПривилегированныйРежим(Истина);
		
		ХранилищеОбщихНастроек.Сохранить("Integration", КлючПараметра, ЗначениеПараметра);
		
		УстановитьПривилегированныйРежим(Ложь);
		
	КонецЕсли
		
КонецПроцедуры

Функция ПрочитатьГлобальныйПараметрНаСервере(КлючПараметра)
	
	Если ЭтоСерверУправляемоеПриложение() Тогда                                                
		
		УстановитьПривилегированныйРежим(Истина);
		
		Результат = ХранилищеОбщихНастроек.Загрузить("Integration", КлючПараметра);
		
		УстановитьПривилегированныйРежим(Ложь);
		
	Иначе
		
		СбисИсключение = НовыйСбисИсключение(500, "МодульОбъектаСервер.ПрочитатьГлобальныйПараметрНаСервере",,,"Ошибка работы с параметрами. Дальнейшая работа не возможна!", Новый ФиксированнаяСтруктура("Ключ", КлючПараметра));
		ВызватьИсключение СбисИсключение_ВСтроку(СбисИсключение);
		
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции

Процедура ЗаполнитьКонтекстВО3ИзКэшаВО2НаСервере(КэшПарам)  Экспорт
	context_params = НастройкиПодключенияПрочитать();
	Если ТипЗнч(context_params) <> Тип("Структура") Тогда
		context_params = Новый Структура;
	КонецЕсли;
	АдрСервера = get_prop(КэшПарам, "АдресСервера");
	АдрСервера = СтрЗаменить(АдрСервера, ".sbis", ".saby");
	Если Прав(АдрСервера, 1) = "/" Тогда
		АдрСервера = Лев(АдрСервера, СтрДлина(АдрСервера) - 1);
	КонецЕсли;
	Если Найти(АдрСервера, "https://") = 0 Тогда
		АдрСервера = "https://" + АдрСервера;
	КонецЕсли;
	context_params.Вставить("ApiUrl", АдрСервера);
	ЗаполнитьПараметрыПодключенияПоУРЛ(context_params);
	context_params.Удалить("Proxy");
	ТипПрокси = get_prop(КэшПарам, "ТипПрокси");
	Если ТипПрокси = "Вручную" Тогда
		ProxyValue = Новый Структура;
		Порт = get_prop(КэшПарам, "ПроксиПорт");
		ТипЧисло  = Новый ОписаниеТипов("Число");
		Порт = ТипЧисло.ПривестиЗначение(Порт);
		АдресСервераСтр	= СтрЗаменить(АдрСервера, ":", Символы.ПС);
		ProxyValue.Вставить("Protocol", СтрПолучитьСтроку(АдресСервераСтр, 1));
		ProxyValue.Вставить("Server", get_prop(КэшПарам, "ПроксиСервер"));
		ProxyValue.Вставить("Port", Порт);
		ProxyValue.Вставить("User", get_prop(КэшПарам, "ПроксиЛогин"));
		ProxyValue.Вставить("Password", get_prop(КэшПарам, "ПроксиПароль"));
		context_params.Вставить("Proxy", Proxy);
		context_params.Вставить("ProxyType", "OtherSettings");
	ИначеЕсли ТипПрокси = "Автоматически" Тогда
		context_params.Вставить("ProxyType", "SystemSettings");
	Иначе
		context_params.Вставить("ProxyType", "NotUse");	
	КонецЕсли;
	
	СпособОбмена = get_prop(КэшПарам, "СпособОбмена");
	ИмяСвойстваТранспорта = ИмяСвойстваТранспорта();
	
	СоответствиеСпособовОбмена = Новый Соответствие;
	СоответствиеСпособовОбмена.Вставить(6, "ExtSdk");
	СоответствиеСпособовОбмена.Вставить(7, "ExtSdkCrypto");
	СоответствиеСпособовОбмена.Вставить(8, "SabyPluginConnector");
	СоответствиеСпособовОбмена.Вставить(9, "SabyHttpsClient");   
	СоответствиеСпособовОбмена.Вставить(1, "SabyConnect");   
	СпособОбменаВО3 = СоответствиеСпособовОбмена.Получить(СпособОбмена);
	Если СпособОбменаВО3 <> Неопределено Тогда
		context_params.Вставить(ИмяСвойстваТранспорта, СпособОбменаВО3);
	ИначеЕсли КэшПарам.ИнтеграцияAPIВызовыНаКлиенте Тогда
		context_params.Вставить(ИмяСвойстваТранспорта, "APIClient");	
	Иначе
		context_params.Вставить(ИмяСвойстваТранспорта, "API");
	КонецЕсли;

	ВходПоСерту =get_prop(КэшПарам, "ВходПоСертификату");
	Если ВходПоСерту Тогда
		context_params.Вставить("AuthType", "Certificate");
		AutoAuth = get_prop(КэшПарам, "ЗапомнитьСертификат");
		Если AutoAuth = Истина Тогда
			context_params.Вставить("Certificate", get_prop(КэшПарам, "Сертификат"));
		КонецЕсли;	
	Иначе
		context_params.Вставить("AuthType", "Login");
		AutoAuth = get_prop(КэшПарам, "ЗапомнитьПароль");
		context_params.Вставить("login", get_prop(КэшПарам, "Логин"));
		Если AutoAuth = Истина Тогда
			context_params.Вставить("password", ШифроватьXOR(get_prop(КэшПарам, "Пароль")));
		КонецЕсли;	
	КонецЕсли;
	context_params.Вставить("AutoAuth", AutoAuth);
	
	context_params.Вставить("Session", Неопределено);
	context_params.Вставить("SessionExtSdk", Неопределено);

	Session = get_prop(КэшПарам, "ИдентификаторСессии");
	ВидТранспорта = ВидТранспорта(context_params);
	СтруктураСессии = Новый Структура;
	СтруктураСессии.Вставить("Session",  Session);
	context_params.Вставить(ИмяСвойстваТранспорта(), ВидТранспорта);
	ТранспортИнтеграции.ЗаписатьСессию(context_params, СтруктураСессии);
		
	context_params.Вставить("EncryptSelectively", get_prop(КэшПарам, "ШифроватьВыборочно", Ложь));
	context_params.Вставить("Timeout", get_prop(КэшПарам, "ВремяОжиданияОтвета", 60));
	context_params.Вставить("ExchangeCatalog", get_prop(КэшПарам, "КаталогОбмена", ""));
	context_params.Вставить("AdvancedLog", get_prop(КэшПарам, "РежимОтладки", Ложь));
	context_params.Вставить("LoggingCatalog", get_prop(КэшПарам, "КаталогОтладки", ""));

	НастройкиПодключенияЗаписать(context_params);
КонецПроцедуры

