
//Функция вызова формы переходов
&НаКлиенте
Функция сбисВыбратьДействие(ВходящийКэш, ПараметрыДействий) Экспорт
	
	Если Кэш = Неопределено Тогда
		Кэш = ВходящийКэш;
	КонецЕсли;
	ЗакрыватьФорму		= Ложь;
	СтруктураПодробнее	= Новый Структура;
	
	
	СбисУстановитьФорму(ПараметрыДействий);
	
	#Если ТолстыйКлиентОбычноеПриложение Тогда
		Возврат ЭтаФорма.ОткрытьМодально();
	#Иначе
		Открыть();
	#КонецЕсли
	
КонецФункции

