
&НаКлиенте
Процедура ОткрытьДокументОнлайн(Команда)
	
	ТипыОшибокЭлем = МодульОбъектаКлиент().ПолучитьЭлементФормыОбработки(ЭтаФорма,"ТипыОшибок");
	ТекущаяСтрока = ТипыОшибокЭлем.ТекущиеДанные;
	
	Если ТекущаяСтрока = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если МодульОбъектаКлиент().МассоваяОперация_Получить(Кэш.РезультатОтправки, "Версия") = 2 Тогда
		Документ1С = ТекущаяСтрока.Документы1С[0].Значение; 
	Иначе
		Документ1С = ТекущаяСтрока.Документ1С;
	КонецЕсли;
	
	фрм = Кэш.ГлавноеОкно.СбисНайтиФормуФункции("СбисПрочитатьСведенияОПакете", Кэш.ФормаРаботыСоСтатусами, "");
	ИдДокументаАккаунта	= фрм.СбисПрочитатьСведенияОПакете(Новый Структура("ДокументСсылка", Документ1С), Кэш);
	
	Если ЗначениеЗаполнено(ИдДокументаАккаунта.ИдДокумента) Тогда
		
		СоставПакета = Кэш.Интеграция.ПрочитатьДокумент(Кэш, ИдДокументаАккаунта.ИдДокумента);
		
		Если СоставПакета<>Ложь Тогда
			Ссылка = СоставПакета.СсылкаДляНашаОрганизация;
		Иначе
			Возврат;
		КонецЕсли;
		
	Иначе
		
		Сообщить("Нет связанного документа Saby");
		Возврат;
		
	КонецЕсли;
	
	ЗапуститьПриложение(Ссылка);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьДокумент1С(Команда)
	
	ТипыОшибокЭлем = МодульОбъектаКлиент().ПолучитьЭлементФормыОбработки(ЭтаФорма,"ТипыОшибок");
	ТекущаяСтрока = ТипыОшибокЭлем.ТекущиеДанные;
	
	Если ТекущаяСтрока = Неопределено Тогда
		Возврат;
	КонецЕсли;  
	
	Если МодульОбъектаКлиент().МассоваяОперация_Получить(РезультатОтправкиПолный, "Версия") = 2 Тогда
		
		СписокСсылок = ТекущаяСтрока.Документы1С;
		
	Иначе
		
		СписокСсылок = Новый СписокЗначений;
		СписокСсылок.Добавить(ТекущаяСтрока.Документ1С);
		
	КонецЕсли;
	
	МодульОбъектаКлиент().СбисОткрытьДокументы(Новый Структура("СписокСсылок", СписокСсылок));
	
КонецПроцедуры

