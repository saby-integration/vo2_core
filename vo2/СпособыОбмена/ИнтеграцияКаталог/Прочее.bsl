
&НаКлиенте
Функция ПолучитьКаталогОтправки(ВходящийКэш=Неопределено) Экспорт
	
	КаталогОтправкиИмя = МодульОбъектаКлиент().ПолучитьЗначениеПараметраСБИС("КаталогОбмена") + "\Отправляемые\";
	КаталогОтправки = Новый Файл(КаталогОтправкиИмя);
	Если Не КаталогОтправки.Существует() Тогда
		СоздатьКаталог(КаталогОтправкиИмя);
	КонецЕсли;
	Возврат КаталогОтправкиИмя;
	
КонецФункции

